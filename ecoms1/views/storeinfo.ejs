<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Store Info</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
  <link rel="stylesheet" href="/styles.css">
  <style>
    body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
}
.back-button {
      order: 1;
      display: inline-block;
      padding: 7px 9px;
      background-color: #0A2472;
      color: #fff;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      position: relative;
      overflow: hidden;
      margin-left: 10px;
    }

    .back-button::before {
      position: absolute;
      top: 0;
      left: 0;
      width: 0;
      height: 100%;
      background-color: #0056b3;
      transition: width 0.3s ease;
    }

    .back-button:hover::before {
      width: 100%;
    }
.back-button:hover{
  background-color: #0d5dbb;
}

    h1 {
      text-align: center; 
      margin: 0 auto; 
      font-size: 30px;
      font-weight: bold;
      margin-top: -30px;
      margin-left: 10px;
    }

    .details {
      max-width: 600px;
      margin: 40px auto;
      padding: 30px;
      background-color: #e6ffe6; 
      border-radius: 12px;
      box-shadow: 8px 10px 8px 10px rgba(0, 0, 0, 0.2);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .details table {
      width: 100%;
      border-collapse: collapse;
      text-align: left;
    }

    .details th, .details td {
      padding: 12px 10px;
      font-size: 20px;
    }

    .details th {
      text-align: left;
      font-weight: bold;
      color: #043b11;
      font-size: 24px;
    }

    .details td {
      color: #333;
    }

    .details td a {
      color: #2e8b57; 
      text-decoration: none;
      transition: color 0.3s;
    }

    .details td a:hover {
      color: #006400; 
    }

    .header {
      margin-top: 1.5rem;
    }
 @media(max-width:890px){
    .app-bar .store-details h4,   .app-bar .edit-profile-dropdown{
      font-size: 20px;
    } 
   }
    @media (max-width: 870px) {
    .app-bar, h1{
        width: 100%;
        min-width: 870px; 
    }
  }
  @media(max-width:860px){
    .details {
       width: 100% !important;
       min-width : 450px !important;
       margin: 20px 100px;
        }
}
@media(max-width:497px){
    .details {
margin-left: 150px;
white-space: nowrap;
        }
}
  </style>
</head>
<body>
  <%- include('./userappbar.ejs') %>
  <div class="header">
    <button onclick="goBack()" class="back-button">Back</button>
    <h1>STORE INFO</h1>
  </div>
  <div class="details">
    <table>
      <tr>
        <th><i class="fas fa-store"></i> Store Name</th>
        <td><%= storeName %></td>
      </tr>
      <tr>
        <th><i class="fas fa-user"></i> Contact Name</th>
        <td><%= storesignin.contactname %></td>
      </tr>
      <tr>
        <th><i class="fas fa-phone-alt"></i> Contact Number</th>
        <td><%= storesignin.mobilenumber %></td>
      </tr>
      <tr>
        <th><i class="fas fa-map-marker-alt"></i> Address</th>
        <td>
          <%= storesignin.place %> 
          <a href="https://www.google.com/maps/search/?api=1&query=<%= encodeURIComponent(storesignin.place) %>" target="_blank">(Show in Map)</a>
        </td>
      </tr>
    </table>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.0.19/dist/sweetalert2.all.min.js"></script>
</body>
</html>
